int main() {
  int i_cnt = 0;
  int len = 10;
  int arr[10];
  write(len);
  write(len);

  i_cnt = 0;
  while (i_cnt < len) {
    arr[i_cnt] = 0;
    i_cnt = i_cnt + 1;
  }
  
  write(len);

  return 0;
}
